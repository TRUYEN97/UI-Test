<UserControl x:Class="UiTest.View.ListBoxItemViews.PropertyItemTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UiTest.View.ListBoxItemViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Name}" Margin="10,0" FontSize="14"/>
        <TextBlock Text="{Binding Value}" Grid.Column="1" 
                FontSize="14"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                TextAlignment="Right"/>
    </Grid>
</UserControl>
