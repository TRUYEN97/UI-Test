<Window x:Class="UiTest.View.Input.InputPropertyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UiTest.View.Input"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="Input" Height="200" Width="300">
    <StackPanel Margin="10">
        <TextBlock Text="Key" Margin="0,0,0,2" HorizontalAlignment="Center"/>
        <TextBox x:Name="Key" Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" HorizontalContentAlignment="Center"/>
        <TextBlock Text="Value" Margin="0,0,0,2" HorizontalAlignment="Center"/>
        <TextBox x:Name="Value" Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" HorizontalContentAlignment="Center" />
        <Button Content="Ok" Command="{Binding Command}" Width="50" HorizontalAlignment="Right" Margin="0,0,5,5"/>
    </StackPanel>
</Window>
