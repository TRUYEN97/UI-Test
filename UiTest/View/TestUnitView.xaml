<UserControl x:Class="UiTest.View.TestUnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UiTest.View.Component"
             mc:Ignorable="d" Height="auto" Width="auto">
    <Border CornerRadius="5" Padding="5" Margin="5" Background="#AA397C98">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <local:LabeledStatusBox Grid.Column="0" Label="Mode" Text="{Binding ModeName}" Padding="3" />
                <local:LabeledStatusBox Grid.Column="1" Label="Time" Text="{Binding TestTime}" Padding="3" />
                <local:LabeledStatusBox Grid.Column="2" Label="Status" Text="{Binding TestTime}" Padding="3"/>
            </Grid>
            <Border  Grid.Row="1" Background="AliceBlue">
                <TabControl ItemsSource="{Binding Tabs}">
                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                    <TabControl.ContentTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{Binding}"/>
                        </DataTemplate>
                    </TabControl.ContentTemplate>
                </TabControl>
            </Border>
        </Grid>
    </Border>
</UserControl>
